# Gitea Runner Registration Token
# Get this from Gitea UI: Settings -> Actions -> Runners -> Create new runner
GITEA_RUNNER_TOKEN=your_registration_token_here

# Podman Socket Path (macOS)
# Find your socket path with: podman machine inspect | grep -i connectionuri
# Example: /var/folders/xx/xxxxxxxxxx/T/podman/podman.sock
# Linux default: /run/podman/podman.sock
PODMAN_SOCKET=/run/podman/podman.sock

# Docker Host (optional - alternative to socket mount)
# macOS Podman Desktop example: unix:///var/folders/xx/xxxxxxxxxx/T/podman/podman.sock
# Leave unset to use the mounted socket
#DOCKER_HOST=unix:///var/run/docker.sock
